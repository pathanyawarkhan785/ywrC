#include <stdio.h>

int main()

{
    int num = 112;
    int mask = 1;

    if (num & mask == 1)
    {
        printf("Odd");
        return 0;
    }
    printf("Even");

    // printf("%d",);
    return 0;
}