#include <stdio.h>

int main()
{
    char ch = 'a';      // Use single quotes for a single character also it stores only 1 character
    printf("%d\n", ch); // when you use %d it returns ascii value
    printf("%c\n", ch);
    return 0;
}
