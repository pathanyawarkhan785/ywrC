#include <stdio.h>
int main()
{
    int a = 5 + 2 % -4; // 2 % -4 = 2 beacuse 2 % 10 = 2 means if first value is lesser than return first value
    printf("%d", a);
    return 0;
}

// % -> B -> O -> D/M -> A/S